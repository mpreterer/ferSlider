import IModelOptions from '../interfaces/IModelOptions';

const defaultSettings: IModelOptions = {
    minValue: 0,
    maxValue: 100,
    step: 1,
    valueFrom: 50,
    valueTo: 100,
    isRange: false,
    isTip: true,
    isBar: true,
    isVertical: false,
};

export default defaultSettings;